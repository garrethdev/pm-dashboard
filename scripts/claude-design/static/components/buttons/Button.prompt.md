`Button` covers every action that isn't the screen's primary one. Labels are muted and brighten on hover, with no accent.

```jsx
<Button>Regenerate</Button>
<Button variant="ghost">Cancel</Button>
```

Use `secondary` beside a `CtaButton` and for row actions. Use `ghost` for dismiss or cancel inside dialogs.
