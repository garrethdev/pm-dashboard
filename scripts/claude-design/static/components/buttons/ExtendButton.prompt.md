`ExtendButton` sits in a proxy or phone row and opens the provider's panel in a new tab. Its label is always "Extend", and it turns accent on hover.

```jsx
<ExtendButton href="https://provider.example/extend/123" />
```

Render nothing when a row has nothing to extend; never show it disabled.
