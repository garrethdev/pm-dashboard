`PeptideMark` is the atom from the logo. It appears at the top of the sidebar rail and on the login screen.

```jsx
<PeptideMark size={24} />
```

It takes the text colour, so it is white on dark and dark on light. Never tint it with the accent.
