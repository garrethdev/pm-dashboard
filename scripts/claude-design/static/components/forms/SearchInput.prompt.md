`SearchInput` filters a table by text. It sits in a `DashCard`'s `actions`.

```jsx
<SearchInput placeholder="Search accounts" />
```

The placeholder names what is searched ("Search accounts", "Profile or username"). Never add helper text under the field.
